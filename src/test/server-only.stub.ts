// Test stub for the Next.js `server-only` marker package. Next provides this at
// build time, but vitest has no Next bundler to resolve it, so the vitest config
// aliases `server-only` to this no-op module. The production guard is unaffected.
export {};
